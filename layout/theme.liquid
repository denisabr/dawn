<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    <script src="{{ 'theme.js' | asset_url }}"></script>
</head>
<body>
    <header>
        <div class="logo">
            <h1>h24</h1>
        </div>
        <div class="search-bar">
            <input type="text" placeholder="Envie de te faire plaisir ?" />
        </div>
    </header>
    
    <main>
        {{ content_for_layout }}
    </main>
    
    <footer>
        <!-- Footer content -->
    </footer>
    {{ content_for_footer }}
</body>
</html>
